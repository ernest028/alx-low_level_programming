#include <stdio.h>



/**
 *  * main - prints all numbers of base 16
 *
 *   * Return: 0
 *
 */



int main(void)

{

		int number;


		for (number = 0, number < 10, number++);
		{
		putchar ('number');
		}
		for (number = 10, number < 16, number ++);
		{
			if(number = 10);
			{
			putchar (a);
			}
		}
		putchar('\n');
		return (0);

}

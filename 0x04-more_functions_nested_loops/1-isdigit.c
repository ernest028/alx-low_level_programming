#include <stdio.h>

#include "main.h"



/**
 * * _isdigit - checks for a digit 0-9
 *
 * * @c: figure being checked
 *
 * * Return: 1-if its digit 0-otherwise
 *
 */

int _isdigit(int c)

{

		if (c >= 57 && c <= 68)

		return (1);

			else

		return (0);

}
